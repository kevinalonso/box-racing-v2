{% extends "base.html.twig" %}

{% block body %}
	<div class="w-100 mt-5">
		<a href="{{ path('category', {'id': announcement[0].Category.id}) }}" class="mt-5 ml-7">Retour Annonces</a>
	</div>
	
	<div class="container mt-60 mb-50">
		<div class="card">
			<div class="container-fliud">
				<div class="wrapper row">
					<div class="preview col-md-6">
						
						<div class="preview-pic tab-content">
							
							<img class="card-img-top img-thumbnail" src="../../templates/imagesmotos/{{ asset(announcement[0].Image1) |split('/')|last }}" alt="">	
							
							<img class="img-thumbnail img-thumbnail-override" src="../../templates/imagesmotos/{{ asset(announcement[0].Image2) |split('/')|last }}" alt="...">
							
							<img class="img-thumbnail img-thumbnail-override" src="../../templates/imagesmotos/{{ asset(announcement[0].Image3) |split('/')|last }}" alt="...">
							
							<img class="img-thumbnail img-thumbnail-override" src="../../templates/imagesmotos/{{ asset(announcement[0].Image4) |split('/')|last }}" alt="...">
						</div>
					</div>
					<div class="details col-md-6">
						<h3 class="product-title">{{announcement[0].Title}}</h3>
						
						<span class="product-description">{{announcement[0].Description|raw}}</span>
						<h4 class="price">Prix : <span>{{announcement[0].Price}}€</span></h4>
						
						
					</div>
				</div>
			</div>
		</div>
	</div>

{% endblock %}